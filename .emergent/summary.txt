<analysis>
**original_problem_statement**: The user wants to implement a cross-platform Share-to-App feature. The current focus is on the iOS implementation. After many iterations, the agreed-upon workflow is to have a native iOS job entry form appear directly within the Share Extension when the user shares a job posting (e.g., from LinkedIn).

PRODUCT REQUIREMENTS:
- **Share Extension Native Form (P0)**:
    - **Goal**: When a user shares a job URL (e.g., from LinkedIn) and selects the CareerFlow app, a native iOS form should appear immediately within the Share Extension.
    - **Functional Requirements**:
        - The form must be pre-populated by parsing the shared content. The agent needs to fetch the URL's HTML content and parse it for key details.
        - **Parsing Logic**:
            - Prioritize Open Graph tags (, ) for instant population of Company Name and Job Title.
            - Use platform-specific selectors (e.g., for LinkedIn's 'top-card' and 'description' sections).
            - Clean the data, ignoring irrelevant text like Promoted or People also viewed.
            - Infer  ('remote'/'hybrid') based on keywords.
            - Extract salary ranges (e.g., 120k - 150k) into min/max numeric values.
            - Parse shared text as a fallback if URL fetching is blocked or provides limited data.
        - **Form Fields**: The form should mirror the app's My Jobs form, including , , , ,  (State/City, hidden for 'Remote'), ,  (pre-filled with today's date), and  (pre-filled with Applied).
    - **Save Functionality**:
        - On Save, the extension must make a direct API call to the backend () to create the new job entry.
        - The user's authentication token must be accessible to the extension via App Groups.
        - After a successful save, a confirmation message should be displayed, and the extension should close.

**User's preferred language**: English

**what currently exists?**
- The app is a full-stack Expo (React Native) and FastAPI application.
- An iOS build with a Share Extension target exists. When a user shares content, a native form UI appears correctly from within the Share Extension.
- A native bridge () is in place to allow the React Native app to share the auth token and backend URL with the Share Extension via App Groups ().
- The  is configured to save the session token and backend URL to the App Group upon user login.
- The  file on the user's local Mac is correctly configured with the Share Extension target and all necessary files and capabilities.

**Last working item**:
    - Last item agent was working: Fixing the parsing logic within the native Share Extension () and ensuring the save functionality works. The previous agent provided a complete, updated  file to the user, which includes URL fetching, Open Graph/HTML parsing, and a corrected save method.
    - Status: IN PROGRESS
    - Agent Testing Done: N
    - Which testing method agent to use? The user must manually apply the provided Swift code on their local Mac, commit the changes from their Mac, build the app using EAS, and test on a physical iOS device via TestFlight. The agent cannot test this feature.
    - User Testing Done: Y (The user tested the previous version and confirmed the form appears but parsing and saving are broken, which prompted the last agent action).

**All Pending/In progress Issue list**:
  Issue 1:  (P0) Parsing logic in the native Share Extension is incorrect.
  Issue 2:  (P0) The Save button in the native Share Extension form fails to create a job entry.
  Issue 3:  (P1) The development workflow between the user's Mac (for native iOS changes) and the Emergent environment (for RN/backend changes) is fragile and has repeatedly caused project configuration to be lost.
  
  Issues Detail:
  - Issue 1: 
     - Attempted fixes: The previous agent completely rewrote  to include a sophisticated parsing strategy. This strategy involves fetching the URL's HTML content, parsing Open Graph () tags for a quick result, and then attempting a deeper parse of the HTML structure and any accompanying shared text.
     - Next debug checklist: 
        1. The user needs to replace the content of their local  with the latest code provided by the last agent.
        2. The user must then build and test.
        3. If parsing still fails, the next step is to debug the  and  functions in . This could involve logging the fetched HTML to see if LinkedIn is blocking the request or returning limited content, and then adjusting the parsing logic accordingly.
     - Why fix this issue and what will be achieved with the fix? This is the core requirement of the feature. Fixing it will auto-populate the job form, creating a seamless user experience.
     - Status:  IN PROGRESS
     - Is recurring issue? Y
     - Should Test frontend/backend/both after fix? User testing on a physical iOS device is required.
     - Blocked on other issue: None.
  - Issue 2: 
     - Attempted fixes: The latest version of  includes an updated  function. It ensures optional fields like salary and location are only included in the API payload if they have values and provides more detailed error alerts based on the API response.
     - Next debug checklist:
        1. After the user applies the new  code, they must test the save functionality.
        2. If it still fails, the user should check the network request logs in Xcode's console while the Share Extension is running to see the exact payload being sent to  and the precise error response from the server.
     - Why fix this issue and what will be achieved with the fix? This will make the entire Share Extension feature functional, allowing users to save jobs directly from the share sheet.
     - Status:  IN PROGRESS
     - Is recurring issue? Y
     - Should Test frontend/backend/both after fix? User testing on a physical iOS device is required.
     - Blocked on other issue: None.
  - Issue 3: 
     - Attempted fixes: The agent has identified the root cause: native Xcode project changes () made on the user's Mac were being overwritten by git operations originating from the Emergent environment. The agent guided the user through a complex usage: git [-v | --version] [-h | --help] [-C <path>] [-c <name>=<value>]
           [--exec-path[=<path>]] [--html-path] [--man-path] [--info-path]
           [-p | --paginate | -P | --no-pager] [--no-replace-objects] [--bare]
           [--git-dir=<path>] [--work-tree=<path>] [--namespace=<name>]
           [--super-prefix=<path>] [--config-env=<name>=<envvar>]
           <command> [<args>]

These are common Git commands used in various situations:

start a working area (see also: git help tutorial)
   clone     Clone a repository into a new directory
   init      Create an empty Git repository or reinitialize an existing one

work on the current change (see also: git help everyday)
   add       Add file contents to the index
   mv        Move or rename a file, a directory, or a symlink
   restore   Restore working tree files
   rm        Remove files from the working tree and from the index

examine the history and state (see also: git help revisions)
   bisect    Use binary search to find the commit that introduced a bug
   diff      Show changes between commits, commit and working tree, etc
   grep      Print lines matching a pattern
   log       Show commit logs
   show      Show various types of objects
   status    Show the working tree status

grow, mark and tweak your common history
   branch    List, create, or delete branches
   commit    Record changes to the repository
   merge     Join two or more development histories together
   rebase    Reapply commits on top of another base tip
   reset     Reset current HEAD to the specified state
   switch    Switch branches
   tag       Create, list, delete or verify a tag object signed with GPG

collaborate (see also: git help workflows)
   fetch     Download objects and refs from another repository
   pull      Fetch from and integrate with another repository or a local branch
   push      Update remote refs along with associated objects

'git help -a' and 'git help -g' list available subcommands and some
concept guides. See 'git help <command>' or 'git help <concept>'
to read about a specific subcommand or concept.
See 'git help git' for an overview of the system. rebase/merge conflict resolution and established a clear workflow.
     - Next debug checklist: The next agent MUST reinforce the correct workflow:
        1. **All native iOS changes (modifying  files, adding targets in Xcode) MUST be done on the user's Mac.**
        2. **The user MUST commit and push these changes from their Mac.**
        3. The agent in the Emergent environment should NOT use any Save to Git feature without first ensuring the user's native changes have been pulled. The safest approach is for the agent to provide code snippets and have the user apply them locally.
     - Why fix this issue and what will be achieved with the fix? Prevents catastrophic loss of work and user frustration. It stabilizes the development process for features requiring native code.
     - Status:  IN PROGRESS
     - Is recurring issue? Y
     - Should Test frontend/backend/both after fix? N/A (Process issue)
     - Blocked on other issue: None.

**In progress Task List**:
  - Task 1: (P0) Finalize and test the native iOS Share Extension with correct parsing and saving.
 Task Detail:
  - Task 1: 
     - Where to resume: The user has been given the latest version of . The next agent should wait for the user to confirm they have applied this code on their Mac and are ready to build/test. The agent should be prepared to debug based on the user's test results.
     - What will be achieved with this? This will complete the user's highest-priority feature, which has been the focus of the last several sessions.
     - Status:  IN PROGRESS
     - Should Test frontend/backend/both after fix? User must test on a physical iOS device.
     - Blocked on something: Waiting for the user to apply the code and test the new build.

**Upcoming and Future Tasks**
  Upcoming Tasks:
    - (P1) Once the iOS Share Extension is confirmed working, verify that the Android share functionality (which uses  and navigates to the  screen) is still functional.
    - (P2) Address the  warning about the unmaintained  library by investigating a modern alternative for the Android implementation.

**Completed work in this session**
- **iOS Share Extension Major Refactor**:
  - Pivoted from a React Native modal approach to a fully native UI form within the Share Extension.
  - Created a complete native job entry form in  with fields for company, position, salary, location, etc.
- **Authentication Bridge**:
  - Updated  to store the user's session token and backend URL into the shared App Group  upon login and clear it on logout.
  - Extended the native bridge ( and ) to include methods (, , ) for this purpose.
- **Git Workflow & Project Recovery**:
  - Diagnosed and resolved numerous critical usage: git [-v | --version] [-h | --help] [-C <path>] [-c <name>=<value>]
           [--exec-path[=<path>]] [--html-path] [--man-path] [--info-path]
           [-p | --paginate | -P | --no-pager] [--no-replace-objects] [--bare]
           [--git-dir=<path>] [--work-tree=<path>] [--namespace=<name>]
           [--super-prefix=<path>] [--config-env=<name>=<envvar>]
           <command> [<args>]

These are common Git commands used in various situations:

start a working area (see also: git help tutorial)
   clone     Clone a repository into a new directory
   init      Create an empty Git repository or reinitialize an existing one

work on the current change (see also: git help everyday)
   add       Add file contents to the index
   mv        Move or rename a file, a directory, or a symlink
   restore   Restore working tree files
   rm        Remove files from the working tree and from the index

examine the history and state (see also: git help revisions)
   bisect    Use binary search to find the commit that introduced a bug
   diff      Show changes between commits, commit and working tree, etc
   grep      Print lines matching a pattern
   log       Show commit logs
   show      Show various types of objects
   status    Show the working tree status

grow, mark and tweak your common history
   branch    List, create, or delete branches
   commit    Record changes to the repository
   merge     Join two or more development histories together
   rebase    Reapply commits on top of another base tip
   reset     Reset current HEAD to the specified state
   switch    Switch branches
   tag       Create, list, delete or verify a tag object signed with GPG

collaborate (see also: git help workflows)
   fetch     Download objects and refs from another repository
   pull      Fetch from and integrate with another repository or a local branch
   push      Update remote refs along with associated objects

'git help -a' and 'git help -g' list available subcommands and some
concept guides. See 'git help <command>' or 'git help <concept>'
to read about a specific subcommand or concept.
See 'git help git' for an overview of the system. issues, including detached HEAD states, merge conflicts, and rebase problems.
  - Recovered the user's corrupted/lost Xcode project configuration () multiple times.
  - Established and communicated the correct, safe workflow for hybrid development between the user's local Mac and the Emergent environment.
- **Build & Submission Fixes**:
  - Resolved an EAS build failure by guiding the user to downgrade the Xcode project's  from 70 to 56.
  - Resolved multiple App Store submission errors related to invalid  values in the Share Extension's .

**Earlier issues found/mentioned but not fixed**
   - The original Android implementation uses , which  flags as unmaintained. This is a technical debt that needs to be addressed after the iOS feature is stable.

**Code Architecture**


**Key Technical Concepts**
- **Native iOS Share Extension**: A separate app target () that provides a UI within the iOS Share Sheet. It's written in Swift and runs independently of the main React Native app.
- **App Groups**: The iOS mechanism for sharing data between a main app and its extensions. Used here to share  for the auth token and backend URL.
- **URL Fetching & Parsing (in Swift)**: The current implementation uses  to fetch the HTML content of a shared job URL and then parses it using a combination of Open Graph tag extraction and regular expressions.
- **Xcode Project Configuration ()**: The critical file that defines build targets, file memberships, and capabilities. This file has been the main source of project corruption and must be carefully managed in usage: git [-v | --version] [-h | --help] [-C <path>] [-c <name>=<value>]
           [--exec-path[=<path>]] [--html-path] [--man-path] [--info-path]
           [-p | --paginate | -P | --no-pager] [--no-replace-objects] [--bare]
           [--git-dir=<path>] [--work-tree=<path>] [--namespace=<name>]
           [--super-prefix=<path>] [--config-env=<name>=<envvar>]
           <command> [<args>]

These are common Git commands used in various situations:

start a working area (see also: git help tutorial)
   clone     Clone a repository into a new directory
   init      Create an empty Git repository or reinitialize an existing one

work on the current change (see also: git help everyday)
   add       Add file contents to the index
   mv        Move or rename a file, a directory, or a symlink
   restore   Restore working tree files
   rm        Remove files from the working tree and from the index

examine the history and state (see also: git help revisions)
   bisect    Use binary search to find the commit that introduced a bug
   diff      Show changes between commits, commit and working tree, etc
   grep      Print lines matching a pattern
   log       Show commit logs
   show      Show various types of objects
   status    Show the working tree status

grow, mark and tweak your common history
   branch    List, create, or delete branches
   commit    Record changes to the repository
   merge     Join two or more development histories together
   rebase    Reapply commits on top of another base tip
   reset     Reset current HEAD to the specified state
   switch    Switch branches
   tag       Create, list, delete or verify a tag object signed with GPG

collaborate (see also: git help workflows)
   fetch     Download objects and refs from another repository
   pull      Fetch from and integrate with another repository or a local branch
   push      Update remote refs along with associated objects

'git help -a' and 'git help -g' list available subcommands and some
concept guides. See 'git help <command>' or 'git help <concept>'
to read about a specific subcommand or concept.
See 'git help git' for an overview of the system..
- **EAS Build with Native Code**: The project uses a bare-like workflow where the  folder is committed to git.  is configured to use this folder directly without running , thus preserving the manual Xcode changes.
- **Hybrid Git Workflow**: A fragile but necessary process where RN/backend code can be modified in Emergent, but all native iOS code and project settings must be modified, committed, and pushed from the user's local Mac.

**key DB schema**
- No changes to the DB schema in this session. The Share Extension interacts with the existing  endpoint which writes to the  collection.

**changes in tech stack**
- No new libraries or dependencies. The changes are primarily in native Swift code and React Native context files.

**All files of reference**
- : The most important file. Contains the entire native UI, parsing logic, and API call for the Share Extension. The user will be manually editing this file on their Mac.
- : The fragile Xcode project file. It's correctly configured on the user's Mac, and the agent must not cause it to be overwritten.
- : Modified to share the session token and backend URL with the native side via the .
-  & : The native module files, updated to handle auth token and backend URL sharing.
- : Updated with the correct, App Store-compliant .

**Critical Info for New Agent**
- **USER FRUSTRATION**: The user is extremely frustrated after a very long and difficult process with many setbacks. Be empathetic, concise, and focused on providing a working solution.
- **DO NOT COMMIT CODE**: Your primary role is to provide the correct Swift code for . The user will manually apply this code on their Mac, commit, and build. **Do not use Save to Git or perform any action that pushes from the Emergent environment**, as this risks overwriting the user's local Xcode project configuration.
- **THE IMMEDIATE TASK**: The user needs the corrected parsing and saving logic for . The previous agent has already provided a comprehensive version of this file. Your first step is to wait for the user to confirm they've applied it and tested the build.
- **DEBUGGING STRATEGY**: If the new code still fails, be prepared to guide the user on how to debug the Share Extension in Xcode. This may involve adding  statements in  and checking the Xcode console to see the fetched HTML and the API request/response.
- **POTENTIAL BLOCKER**: LinkedIn may be blocking or rate-limiting the  request from the Share Extension. If the fetched HTML is empty or a login page, the parsing will fail. The fallback is to rely only on parsing the  that LinkedIn provides, which is less rich but more reliable. You should have this fallback plan ready.
- **REINFORCE WORKFLOW**: Gently remind the user that all native iOS changes must be made and committed from their Mac to avoid the project-breaking issues experienced previously.

**documents created in this job**
- No new documents were created. The  from the previous session is now partially outdated due to the pivot to a fully native UI.

**Last 10 User Messages and any pending HUMAN messages** 
1.  **User (Latest):** Confirmed everything is working except the parsing and saving. Provided very specific requirements for how a LinkedIn post should be parsed and requested the code to be provided without any git commits. (Status: ADDRESSED, agent provided the code).
2.  **User:** Changed the  code and asked what to do next. (Status: ADDRESSED, agent provided git/build steps).
3.  **User:** Everything is working now, but parsing is still not correct. Attached a screenshot of a LinkedIn job post and gave precise instructions on how each field should be mapped. (Status: ADDRESSED, agent provided new code with improved parsing).
4.  **User:** Asked for the  code without any git commits. (Status: ACKNOWLEDGED).
5.  **User:** The An Expo user account is required to proceed.

Log in to EAS with email or username (exit and run eas login --help to see other login options)
Input is required, but stdin is not readable. Failed to display prompt: Email or username command failed with an unexpected argument: --skip-doctor. (Status: ADDRESSED, agent provided alternative flags).
6.  **User:** An Expo user account is required to proceed.

Log in to EAS with email or username (exit and run eas login --help to see other login options)
Input is required, but stdin is not readable. Failed to display prompt: Email or username failed with a generic error message. (Status: ADDRESSED, agent provided debugging steps).
7.  **User:** The app is not in the share sheet again. Stated they are frustrated. (Status: ADDRESSED, agent identified the root cause as the activation rule and provided a fix).
8.  **User:** The build failed due to . Asked if they should change it to 56. (Status: ADDRESSED, agent confirmed and provided command).
9.  **User:** Force push succeeded, asked if they can run An Expo user account is required to proceed.

Log in to EAS with email or username (exit and run eas login --help to see other login options)
Input is required, but stdin is not readable. Failed to display prompt: Email or username. (Status: ADDRESSED, agent confirmed).
10. **User:** Asked how to check Target Membership in Xcode before pushing. (Status: ADDRESSED, agent provided detailed steps).

**Project Health Check:**
- **Broken:**
    - The core Share-to-App feature is non-functional due to incorrect parsing and a failing save mechanism in the native Share Extension.
- **Mocked:**
    - None.

**3rd Party Integrations**
- ****: Used for the Android part of the Share-to-App feature. It is flagged as unmaintained by .

**Testing status**
  - Testing agent used after significant changes: NO
  - Troubleshoot agent used after agent stuck in loop: YES (Used to identify why the app wasn't opening from the share extension, leading to a change in strategy).
  - Test files created: None.
  - Known regressions: The Share Extension functionality has been repeatedly lost and restored due to git/build process issues.

**Credentials to test flow:**
N/A. The user is performing all builds and testing on their own Mac and iPhone.

**What agent forgot to execute**
- The agent and user repeatedly struggled with a stable Git workflow, leading to the user's Xcode project configuration being overwritten multiple times. The root cause (EAS prebuild wiping the  folder, or git conflicts from multiple development environments) was identified late in the process, causing significant user frustration and rework. The current workflow (user-managed native changes on Mac) is the resolution.
</analysis>
